# GitHub Commands

This file contains commonly used GitHub commands to manage repositories,
branches, and remote operations.

---

## 🔗 GitHub Repository Commands

### 1. Clone a GitHub repository
```bash
git clone <repository-url>
2. Check remote repository
git remote -v
3. Add remote GitHub repository
git remote add origin <repository-url>
4. Remove remote repository
git remote remove origin
⬆️ Push Commands
5. Push code to GitHub
git push origin main
6. Push code to specific branch
git push origin abt
⬇️ Pull Commands
7. Pull latest changes from GitHub
git pull origin main
8. Pull with rebase
git pull origin abt --rebase
🌿 Branch Commands
9. List all branches
git branch
10. Create new branch
git branch bash
11. Switch branch
git checkout bash
12. Create and switch branch
git checkout -b gith
🧹 Useful Commands
13. Check commit history
git log
14. Check short commit history
git log --oneline
15. Check repository status
git status
📌 Note
These commands are used to interact with GitHub remote repositories
and manage project versions efficiently.
# Task1esm
# hello krishu
# kkkk
<h1> krish <h1>
THESE WAS Final change 

